<UserControl x:Class="BankQueue.View.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:BankQueue.ViewModel"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModel:AdminViewModel}"
             d:DesignHeight="100" Margin="3" d:DesignWidth="150" BorderBrush="Gray" BorderThickness="2">
    <Grid Margin="3">
         <Grid.RowDefinitions>
             <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>  
        
        <Label Content="ADMIN"></Label>
        <ToggleButton Grid.Row="1" Content="Start | Stop" Command="{Binding StartStopCommand, Mode=OneTime}" />
        <Label Grid.Row="2" Content="{Binding BankId, Mode=OneTime, StringFormat=BankID:{0}}" />
        <Label Grid.Row="3" Content="{Binding Status, Mode=OneWay}" />
        <TextBox Grid.Row="4" Text="{Binding BankName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
