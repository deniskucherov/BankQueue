<UserControl x:Class="BankQueue.View.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:BankQueue.ViewModel"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModel:AdminViewModel}"
             d:DesignHeight="50" d:DesignWidth="150">
    <Grid>
         <Grid.RowDefinitions>
             <RowDefinition Height="Auto" />
             <RowDefinition Height="Auto" />
             <RowDefinition Height="*" />
         </Grid.RowDefinitions>  
        
        <ToggleButton Content="Start/End" Command="{Binding StartStopCommand}" />
        <Label Grid.Row="1" Content="Bank Id: " />
        <TextBox Grid.Row="2" Text="Bank Name"/>
    </Grid>
</UserControl>
